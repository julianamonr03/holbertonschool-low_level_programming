#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 * print alphabet
 * Return: Always 0
 */
int main(void)
{
char al;
for (al = 'a'; al <= 'z'; al++)
putchar(al);
putchar('\n');
return (0);
}
